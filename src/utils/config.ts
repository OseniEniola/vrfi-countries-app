const BASE_URL = import.meta.env.VITE_BASE_URL;
const WHITELISTED_OBJ_FIELDS = [
  'name',
  'region',
  'flags',
  'currency',
  'continent',
  'population',
];

export { BASE_URL , WHITELISTED_OBJ_FIELDS};
